# Set reference values for each parameter

Set reference values for each parameter

## Usage

``` r
parseq_reference(mod, auto = TRUE)
```

## Arguments

- mod:

  a model object.

- auto:

  if `TRUE` then the model parameter list is used.
